/**
 * Photo category mapping: display name (used in `photos/data.json`) → URL slug.
 * Add a new line here when you want a new photo category.
 */
export const photoCategories: Record<string, string> = {
  雪山: 'snow',
  夕阳: 'sunset',
  街景: 'street',
}