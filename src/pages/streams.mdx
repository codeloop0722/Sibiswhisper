---
title: Streams
subtitle: 视频 / 播客 / 直播记录
description: 我的视频、播客与直播时间线
bgType: false
ogImage: fallback
---

import BaseLayout from '~/layouts/BaseLayout.astro'
import TabbedLayout from '~/layouts/TabbedLayout.astro'
import ListView from '~/components/views/ListView.astro'

<BaseLayout
  title={frontmatter.title}
  description={frontmatter.description}
  ogImage={frontmatter.ogImage}
>
  <TabbedLayout subtitle={frontmatter.subtitle}>
    <ListView collectionType="streams" pageToc={true} />
  </TabbedLayout>
</BaseLayout>
